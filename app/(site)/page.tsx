/*
 * Homepage. Phase 0 placeholder: it exists so the foundation (tokens, fonts,
 * reset, routing) can be verified on a real route. The section sequence fixed
 * in SOURCE_OF_TRUTH.md §3 is assembled here from Phase 1 onward.
 */
export default function HomePage() {
  return (
    <main>
      <h1>Assetly</h1>
      <p>Access . Scale . Grow</p>
    </main>
  );
}
